<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ระบบติดตามคำสัญญา</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Sarabun', sans-serif; background-color: #f4f7f6; }
        .navbar { background-color: #1a237e !important; }
        .promise-card { border: none; border-radius: 15px; transition: 0.3s; }
        .promise-card:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.1); }
        .badge-status { font-size: 0.85rem; padding: 0.5em 1em; }
    </style>
</head>
<body>

<nav class="navbar navbar-dark shadow-sm mb-4">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/">📌 Political Tracker</a>
        <div class="d-flex align-items-center text-white">
            <% if (user) { %>
                <span class="me-3">สวัสดี, <%= user.name %></span>
                <a href="/auth/logout" class="btn btn-outline-light btn-sm">ออกจากระบบ</a>
            <% } else { %>
                <a href="/login" class="btn btn-light btn-sm text-primary fw-bold">Login สำหรับเจ้าหน้าที่</a>
            <% } %>
        </div>
    </div>
</nav>

<div class="container">
    <div class="row mb-4">
        <div class="col">
            <h2 class="fw-bold">รายการคำสัญญาหาเสียง</h2>
            <p class="text-muted">ตรวจสอบความคืบหน้าของนโยบายที่คุณเลือก</p>
        </div>
    </div>

    <div class="row">
        <% promises.forEach(p => { %>
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card h-100 promise-card shadow-sm">
                    <div class="card-body d-flex flex-column">
                        <div class="mb-2">
                            <span class="badge rounded-pill 
                                <%= p.status === 'เงียบหาย' ? 'bg-danger' : 
                                   p.status === 'กำลังดำเนินการ' ? 'bg-warning text-dark' : 'bg-success' %>">
                                <%= p.status %>
                            </span>
                        </div>
                        <h5 class="card-title fw-bold"><%= p.politician_name %></h5>
                        <p class="card-text text-secondary flex-grow-1"><%= p.details %></p>
                        <hr>
                        <div class="d-grid">
                            <a href="/promises/<%= p.promise_id %>" class="btn btn-outline-primary rounded-pill">ดูรายละเอียด</a>
                        </div>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</div>
</body>
</html>