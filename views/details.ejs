<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <title>รายละเอียดคำสัญญา</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .timeline { border-left: 3px solid #0d6efd; position: relative; list-style: none; padding-left: 20px; }
        .timeline-item { position: relative; margin-bottom: 30px; }
        .timeline-item::before { 
            content: ""; position: absolute; width: 16px; height: 16px; 
            background-color: #0d6efd; border-radius: 50%; left: -30px; top: 5px; 
        }
    </style>
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="card shadow-sm mb-4 border-0">
            <div class="card-body p-4">
                <nav aria-label="breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="/">หน้าหลัก</a></li><li class="breadcrumb-item active">รายละเอียด</li></ol></nav>
                <h2 class="fw-bold text-primary mb-3"><%= promise.politician_name %></h2>
                <p class="lead"><%= promise.details %></p>
                <div class="d-flex align-items-center">
                    <span class="badge bg-info text-dark me-2">พรรค: <%= promise.party %></span>
                    <span class="badge <%= promise.status === 'เงียบหาย' ? 'bg-danger' : 'bg-success' %>">สถานะ: <%= promise.status %></span>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-8">
                <h4 class="fw-bold mb-4">ประวัติความคืบหน้า</h4>
                <% if (updates.length > 0) { %>
                    <div class="timeline">
                        <% updates.forEach(update => { %>
                            <div class="timeline-item">
                                <div class="card shadow-sm border-0">
                                    <div class="card-body">
                                        <small class="text-primary fw-bold"><%= update.update_date.toLocaleDateString('th-TH') %></small>
                                        <p class="mb-0 mt-1"><%= update.update_details %></p>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } else { %>
                    <div class="alert alert-light text-center border">ยังไม่มีข้อมูลการอัปเดตความคืบหน้า</div>
                <% } %>
            </div>

            <div class="col-md-4">
                <% if (user && user.role === 'admin') { %>
                    <div class="card border-warning">
                        <div class="card-body">
                            <h5 class="fw-bold">จัดการข้อมูล</h5>
                            <% if (promise.status !== 'เงียบหาย') { %>
                                <a href="/promises/<%= promise.promise_id %>/update" class="btn btn-warning w-100">เพิ่มความคืบหน้า</a>
                            <% } else { %>
                                <p class="text-danger small mb-0">* สถานะเงียบหาย ไม่สามารถอัปเดตเพิ่มได้</p>
                            <% } %>
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>